spring:
  application:
    name: backend
  datasource:
    url: jdbc:mysql://localhost:3306/inmob_db
    username: root
    password: mysql
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.MySQLDialect
  security:
    oauth2:
      resourceserver:
        jwt:
          issuer-uri: https://dev-1kd2aymzw2vebr77.us.auth0.com/
          audiences: https://inmob2-api
